external_components:
  - source: github://luar123/zigbee_esphome
    components:
      - zigbee

zigbee:
  router: true
  components: all
  on_join:
    then:
      logger.log:
        level: VERBOSE
        format: "Joined network"
        tag: "zigbee"

captive_portal: !remove
wifi:
  ap: !remove